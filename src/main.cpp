#include "application.h"

int main(int argc, char *argv[]) {
        // Application Creation
        Application application;
        
        // Main Loop
        application.run();
        return 0;
}